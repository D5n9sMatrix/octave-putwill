#!/usr/bin/octave

# The departure of the games makes necessary the variables
# of the path in which it is found in the constructive analysis
# of intervals on numerical facts for verbalized excuses of
# mathematics for support of the time in reading of numerical
# base consolidated by colloquial numerical structures using
# solid base to make the mathematics sense.

function games(list, will)

  list = 40
  will = 80

  if list != will
     type -q list
  else
     disp("Pine Header Will")
     type -q list
     disp("Pine Arms Will")
  endif

end
