#!/usr/bin/octave
# Its quality of expressions in arguments attributes to the case
# of complex informality of the matrices informing the numerical
# notions in which the base of the mirror refers to introductions
# of mathematics on the matrix of verbalized collocations in the
# simplest form of movement conductors to contextualize the
# personality of the environment graph, use this to maintain
# the rules of matches in the area of mathematics.

function output(out, will)
  out = 10
  will = 4
  if out != will
     disp("matrices logical form")
  else
     out * will
  endif
end

