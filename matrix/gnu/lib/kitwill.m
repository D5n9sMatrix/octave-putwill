#!/usr/bin/octave

# The bases of the extremists call in the case of math comics
# cartoons using a loop over the numerical stores of the scientific
# analysis variables for constructive numerical methods to strengthen
# the solid foundation of the fragile situations of the mathematics
# of the slips so that their invoices are easy and objective.

function comics(kit, will, call, z)

  kit = 40
  will = 80
  call = 0
  z = 4

  if kit != will
     call =  all([2, 4; 3, 0])
     call = angle(z)
     type -q will
  endif

end
